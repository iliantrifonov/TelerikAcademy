Rebuild solution as all bin folders have been deleted due to size issues.

In each project, you have to modify the <connectionStrings> for the server for it to work ! 

Also packages are deleted, you may need to restore them manualy if nuget does not automatically.